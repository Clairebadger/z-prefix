{
  "name": "server",
  "version": "1.0.0",
  "description": "server for z prefix",
  "main": "index.js",
  "scripts": {
    "start": "knex migrate:rollback && knex migrate:latest && knex seed:run && node ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "knex": "^2.1.0",
    "pg": "^8.7.3"
  },
  "author": "",
  "license": "ISC"
}

